const AppConfig = {
  api: {
    vtuber: {
        getAll: "/vtuber"
    },
    organization: {
        getAll: "/org"
    },
    meadRecipe: {
        getAll: "/meadRecipes"
    }
  }
};

export default AppConfig;
